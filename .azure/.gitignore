# Ignore deployment outputs that may contain sensitive information
deployment-outputs-*.json
deployment-output-temp.json
*.env

# Ignore GitHub secrets file (contains sensitive credentials)
GITHUB_SECRETS.txt

# Allow deployment instructions (no sensitive data)
!DEPLOYMENT_INSTRUCTIONS.md
