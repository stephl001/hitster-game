name: Deploy Backend to Dev

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
      - '.github/workflows/deploy-dev-backend.yml'
    tags-ignore:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: read
  checks: write
  pull-requests: write

jobs:
  deploy-backend:
    runs-on: ubuntu-latest
    environment: dev

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          global-json-file: ./backend/global.json

      - name: Restore dependencies
        run: dotnet restore
        working-directory: ./backend

      - name: Build
        run: dotnet build --configuration Release --no-restore
        working-directory: ./backend

      - name: Run tests
        run: dotnet test --configuration Release --no-build --verbosity normal --logger "trx;LogFileName=test-results.trx" --collect:"XPlat Code Coverage"
        working-directory: ./backend/SongsterGame.Tests

      - name: Publish test results
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: Backend Tests (Dev)
          path: './backend/SongsterGame.Tests/TestResults/*.trx'
          reporter: dotnet-trx
          fail-on-error: true

      - name: Publish code coverage
        uses: codecov/codecov-action@v4
        if: always()
        with:
          files: './backend/SongsterGame.Tests/TestResults/**/coverage.cobertura.xml'
          flags: backend
          name: backend-coverage
          fail_ci_if_error: false

      - name: Publish
        run: dotnet publish --configuration Release --no-build --output ./publish
        working-directory: ./backend/SongsterGame.Api

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy to Azure App Service
        uses: azure/webapps-deploy@v3
        with:
          app-name: app-songster-api-dev
          package: ./backend/SongsterGame.Api/publish

      - name: Backend deployment summary
        run: |
          echo "### Backend Deployment Complete (Dev)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Environment:** dev" >> $GITHUB_STEP_SUMMARY
          echo "**App Service:** app-songster-api-dev" >> $GITHUB_STEP_SUMMARY
          echo "**URL:** https://app-songster-api-dev.azurewebsites.net" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Tests:** See test results above" >> $GITHUB_STEP_SUMMARY
